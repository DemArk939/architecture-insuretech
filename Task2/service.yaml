apiVersion: v1
kind: Service
metadata:
  name: test-app-service
  labels:
    app: test-app
spec:
  type: NodePort
  selector:
    app: test-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      name: http
